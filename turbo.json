{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": ["**/.env.*local"],
    "globalEnv": [
        "NEXT_PUBLIC_ENVIRONMENT_NAME",
        "NEXT_PUBLIC_PROCESS_DETAIL_REFETCH_INTERVAL",
        "NEXT_PUBLIC_ORCHESTRATOR_API_HOST",
        "NEXT_PUBLIC_ORCHESTRATOR_API_PATH",
        "NEXT_PUBLIC_ORCHESTRATOR_GRAPHQL_HOST",
        "NEXT_PUBLIC_ORCHESTRATOR_GRAPHQL_PATH"
    ],
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
        },
        "test": {},
        "lint": {
            "dependsOn": ["^build"]
        },
        "tsc": {
            "dependsOn": ["^build"]
        },
        "dev": {
            "cache": false,
            "persistent": true
        },
        "reset": {
            "dependsOn": ["^reset"]
        }
    }
}
