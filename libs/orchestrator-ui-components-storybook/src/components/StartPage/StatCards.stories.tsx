import type { Meta } from '@storybook/react';
import { StatCards } from '@orchestrator-ui/orchestrator-ui-components';

const Story: Meta<typeof StatCards> = {
    component: StatCards,
    title: 'StartPage/StatCards',
};
export default Story;

export const Primary = {
    args: {},
};
