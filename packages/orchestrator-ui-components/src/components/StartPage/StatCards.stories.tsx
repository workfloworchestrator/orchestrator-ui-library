import type { Meta } from '@storybook/react';
import { StatCards } from './StatCards';

const Story: Meta<typeof StatCards> = {
    component: StatCards,
    title: 'StartPage/StatCards',
};
export default Story;

export const Primary = {
    args: {},
};
