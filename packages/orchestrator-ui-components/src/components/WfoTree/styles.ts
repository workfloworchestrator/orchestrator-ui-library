export const getStyles = () => {
    const expandIconContainer = {
        cursor: 'pointer',
    };

    const treeContainer = {
        width: 0,
        marginTop: 8,
        marginRight: -8,
    };

    return {
        expandIconContainer,
        treeContainer,
    };
};
