import { WfoCode } from '@orchestrator-ui/orchestrator-ui-components';
import type { Meta } from '@storybook/react';

const Story: Meta<typeof WfoCode> = {
    component: WfoCode,
    title: 'Icons/WfoCode',
};
export default Story;

export const Primary = {
    args: {},
};
